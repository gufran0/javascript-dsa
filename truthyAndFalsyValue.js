Boolean(0);
Boolean(NaN);
// false
// in numbers only 0 and nan is false;

Boolean("");
// empty string is false
// empty string is not false

Boolean(undefined);
// false
Boolean(true);
// true

// 0 | nan | null | undefined

!0;
// changes the value
!!0;
// chnages it back to what it is
